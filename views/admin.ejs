<%- include('./partials/header') %>
<div class="w-full h-screen flex items-start px-20 py-20">
    <div class="w-[25%] flex h-screen flex-col items-start">
        <div class="flex flex-col space-y-4">
            <a class="block w-fit text-lg font-semibold text-gray-700 hover:text-blue-500 transition" href="#">All Products</a>
            <a class="block w-fit text-lg font-semibold text-gray-700 hover:text-blue-500 transition" href="#">Create New Product</a>
        </div>
    </div>

    <!-- Main Product Section -->
    <div class="w-[75%] flex flex-col gap-8">
        <!-- Delete All Link -->
        <a class="text-red-500 text-lg font-semibold hover:text-red-700 transition" href="#">Delete All</a>
        
        <!-- Product Grid -->
        <div class="grid grid-cols-4 gap-6">
            <!-- Product Card -->
            <% for (let i = 0; i < 8; i++) { %>
            <div class="bg-white rounded-lg shadow-lg overflow-hidden transition-transform transform hover:scale-105">
                <div class="w-full h-40 bg-gray-200 flex justify-center items-center">
                    <!-- Placeholder for Image -->
                    <img src="https://via.placeholder.com/150" alt="Product Image" class="h-full object-cover">
                </div>
                <div class="p-4 flex justify-between items-center">
                    <div>
                        <h3 class="text-lg font-semibold text-gray-800">Clinge Bag</h3>
                        <h4 class="text-md font-semibold text-green-600">₹ 1200</h4>
                    </div>
                    <a class="w-8 h-8 flex items-center justify-center rounded-full bg-blue-500 text-white hover:bg-blue-700 transition" href="#">
                        <i class="ri-add-line"></i>
                    </a>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</div>

<%- include('./partials/footer') %>
